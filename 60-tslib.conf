#
# Catch-all tslib loader for udev-based systems
#
# We match on all touchscreen devices.

Section "InputClass"
	Identifier "tslib touchscreen catchall"
	MatchIsTouchscreen "on"
	MatchDevicePath "/dev/input/*"
	Driver "tslib"
EndSection
